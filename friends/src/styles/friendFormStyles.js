import {
  makeStyles
} from "@material-ui/core/styles";

export const friendFormStyles = makeStyles({
  root: {
    display: "flex",
    flexDirection: "column",
    border: "2px solid black",
    width: "300px",
    // height: "300px",
    // alignContent: "space-around"
  }
})